%h3 Listing users

%table.table.table-striped
  %tr
    %th Username
    %th Password
    %th Address
    %th Phone
    %th Email
    %th Is admin
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.password
      %td= user.address
      %td= user.phone
      %td= user.email
      %td= user.is_admin
      %td= link_to 'Show', user, :class => 'btn btn-mini btn-default btn-xs'
      %td= link_to 'Edit', edit_user_path(user), :class => 'btn btn-mini btn-default btn-xs'
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-mini btn-danger btn-xs'

%br

= link_to 'New User', new_user_path, :class => 'btn btn-mini btn-default btn-xs'
